[pytest]
markers =
    smoke: fast, critical-path checks (login, core navigation)
    regression: full functional coverage (cart, checkout, sorting, etc.)

# Default browser + headless mode for local runs; overridden in CI matrix
addopts = --browser chromium
